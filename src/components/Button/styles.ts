import { styled } from "@mui/material/styles";
import MuiButton from "@mui/material/Button";
import { ComponentType } from "react";

export const StyledButton: ComponentType<any> = styled(MuiButton)`
  text-transform: none;
`;
